async function apiGet(action,params={}){const url=new URL(API_URL);url.searchParams.set("action",action);Object.entries(params).forEach(([k,v])=>url.searchParams.set(k,v));const response=await fetch(url.toString(),{method:"GET",redirect:"follow",cache:"no-store"});const raw=await response.text();if(!response.ok)throw new Error("HTTP "+response.status+": "+raw.substring(0,200));let data;try{data=JSON.parse(raw)}catch(e){throw new Error("Invalid JSON response: "+raw.substring(0,200))}if(data.ok!==true)throw new Error(data.error||"API returned ok=false");return data}