const $=id=>document.getElementById(id);async function loadTests(){try{const d=await apiGet("getTests");$("tests").innerHTML=d.tests.map(t=>`<option value="${t.testId}">${t.testName}</option>`).join("")}catch(e){$("msg").textContent=e.message}}$("start").onclick=()=>{const name=$("name").value.trim(),mobile=$("mobile").value.trim(),testId=$("tests").value;if(!name||!/^\d{10}$/.test(mobile)){ $("msg").textContent="Enter name and valid 10 digit mobile number.";return}sessionStorage.setItem("tetStudent",JSON.stringify({name,mobile,testId}));location.href="exam.html"};loadTests();